# Deployments

Contract addresses per network. Public — no secrets.

- `testnet.json` — Stellar testnet + Base Sepolia addresses. Updated as sprint stories ship.
- Private key material lives in `.env.local` at the repo root (gitignored). Never commit.

{
  "name": "niet",
  "version": "0.1.0",
  "private": true,
  "description": "Conditional settlement layer for cross-chain intents on Stellar",
  "license": "MIT",
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "pnpm -r --filter './ts/**' typecheck",
    "test": "pnpm -r --filter './ts/**' test",
    "check": "pnpm typecheck && pnpm test"
  }
}
